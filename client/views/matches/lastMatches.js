/* ---------------------------------------------------- +/

## lastMatches ##

Code related to the lastMatches template

/+ ---------------------------------------------------- */

Template.lastMatches.created = function () {
  //
};

Template.lastMatches.helpers({
//

});

Template.lastMatches.rendered = function () {
  //
};

Template.lastMatches.events({
  //
});